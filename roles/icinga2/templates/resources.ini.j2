[icingaweb2]
type = "db"
db = "mysql"
host = "localhost"
port = "3306"
dbname = "{{ icingaweb2_db }}"
username = "{{ icingaweb2_user }}"
password = "{{ icingaweb2_password }}"

[icinga2]
type = "db"
db = "mysql"
host = "localhost"
port = "3306"
dbname = "{{ icinga2_db.stdout }}"
username = "{{ icinga2_user.stdout }}"
password = "{{ icinga2_password.stdout }}"
